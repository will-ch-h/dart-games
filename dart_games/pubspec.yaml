name: dart_games
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.6.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  provider: ^6.1.1

  # HTTP requests for API calls
  http: ^1.1.0

  # Secure storage for bearer token
  flutter_secure_storage: ^9.0.0

  # URL launcher for OAuth redirect
  url_launcher: ^6.2.2

  # WebView for OAuth (alternative)
  webview_flutter: ^4.4.4

  # Google Sign-In for OAuth
  google_sign_in: ^6.2.1

  # Text-to-speech for dartboard announcer
  flutter_tts: ^4.1.0

  # UUID generation for Scolia API messages
  uuid: ^4.5.1

  # Image picker for player photos
  image_picker: ^1.0.7

  # Path provider for storing photos
  path_provider: ^2.1.2

  # Confetti animation
  confetti: ^0.7.0

  # Audio player for victory music
  audioplayers: ^6.1.0

  # File picker for selecting custom victory music
  file_picker: ^8.1.4

  # Google Fonts for Nunito typography
  google_fonts: ^6.1.0

  # WebSocket for Scolia dartboard connection
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Integration testing for UI automation
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

  # Icon generator
  flutter_launcher_icons: ^0.13.1
  web: ^1.1.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # Shared/common assets used across all games
    - assets/common/icons/
    - assets/common/images/

    # Carnival Derby game assets
    - assets/games/carnival_derby/icons/
    - assets/games/carnival_derby/images/
    - assets/games/carnival_derby/sounds/

    # Monster Mash game assets
    - assets/games/monster_mash/icons/
    - assets/games/monster_mash/images/
    - assets/games/monster_mash/characters/
    - assets/games/monster_mash/sounds/

    # Reef Royale game assets
    - assets/games/reef_royale/icons/
    - assets/games/reef_royale/images/
    - assets/games/reef_royale/characters/
    - assets/games/reef_royale/corals/
    - assets/games/reef_royale/sounds/

    # Target Tag game assets
    - assets/games/target_tag/icons/
    - assets/games/target_tag/sounds/TargetTag-BananaSlip.mp3
    - assets/games/target_tag/sounds/TargetTag-Blink.mp3
    - assets/games/target_tag/sounds/TargetTag-Choir.mp3
    - assets/games/target_tag/sounds/TargetTag-Dream.mp3
    - assets/games/target_tag/sounds/TargetTag-Fanfare.mp3
    - assets/games/target_tag/sounds/TargetTag-Launch.mp3
    - assets/games/target_tag/sounds/TargetTag-Magical.mp3
    - assets/games/target_tag/sounds/TargetTag-Ominous.mp3
    - assets/games/target_tag/sounds/TargetTag-PianoRoll.mp3
    - assets/games/target_tag/sounds/TargetTag-Spring.mp3
    - assets/games/target_tag/sounds/TargetTag-Swipe.mp3
    - assets/games/target_tag/sounds/TargetTag-Teasing.mp3
    - assets/games/target_tag/sounds/TargetTag-Villain.mp3
    - assets/games/target_tag/sounds/TargetTag-Whistle.mp3
    - assets/games/target_tag/sounds/TargetTag-WindUp.mp3

    # Clockwork Quest game assets
    - assets/games/clockwork_quest/icons/
    - assets/games/clockwork_quest/images/
    - assets/games/clockwork_quest/images/characters/
    - assets/games/clockwork_quest/images/gears/
    - assets/games/clockwork_quest/sounds/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Icon configuration
flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: "assets/common/images/logo.png"
    background_color: "#8B4513"
    theme_color: "#FFC107"
  image_path: "assets/common/icons/icon.png"
