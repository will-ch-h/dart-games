# Perf Audit Plan — 2026-05-05 [scope: full]

> **Status:** Phase 5 — complete (8 applied / 3 deferred)
> **Approved findings:** "approve all" → applied: [A1, A2, A3, A4, A5, B1, B2, C1]; deferred-on-second-look: [C2, C3, C4]
> **Implementation log:** (see section at bottom — final state)
> **Final test gate:** `flutter test` 1306/1306 ✅ · `cd server && dart test` 190/190 ✅ · `flutter analyze` 0 new errors ✅

## Summary

17 findings across the catalog (N/C/R/D/M/T/S). Bucket counts: **A=5, B=2, C=4, D=6**.

Top 3 (highest ROI):
1. **N3** — Per-player `updatePlayerStats` loop in all 6 results screens → batch endpoint. Saves `(N − 1)` HTTP round-trips per game (e.g. 7 RTTs for an 8-player Reef Royale match).
2. **R5** — None of the 6 game screens wrap their continuous-animation subtrees in `RepaintBoundary`. Adding it stops repaint cascades from dirtying sibling subtrees on every animation frame.
3. **D5 + D3** — `PRAGMA synchronous = NORMAL` is missing and `game_history.player_id` has no index. The history GET is the hottest server query; both fixes compound for results-screen latency.

## Bucket A — High impact, low effort, low risk (recommended)

### A1 [N3] Batch per-player updatePlayerStats across all 6 results screens

**Game code impact:**
- **Files (and line ranges):**
  - `lib/screens/games/reef_royale/reef_royale_results_screen.dart:113-128` — replace per-player loop with single batch call
  - `lib/screens/games/target_tag/target_tag_results_screen.dart:118-133` — same
  - `lib/screens/games/carnival_horse_race/horse_race_results_screen.dart:143-158` — same
  - `lib/screens/games/clockwork_quest/clockwork_quest_results_screen.dart:72-86` — same
  - `lib/screens/games/lunar_lander/lunar_lander_results_screen.dart:95-104` — same
  - `lib/screens/games/monster_mash/monster_mash_results_screen.dart:147-162` — same
  - `lib/providers/player_provider.dart` — add `batchUpdatePlayerStats(List<PlayerStatsUpdate>)` (after the current `updatePlayerStats` at line 255). Internally calls `_api.batchAddPlayerHistory(...)` once.
  - `lib/services/api/api_client.dart` — add `batchAddPlayerHistory(List<Map<String, dynamic>>)` POSTing to `/api/v1/players/history/batch`.
  - `server/lib/routes/player_routes.dart` — add `POST /history/batch` handler that wraps the inserts in a single `db.transaction`.
- **Currently** (`reef_royale_results_screen.dart:113-128`):
  ```dart
  for (final playerId in currentGame.playerIds) {
    if (!mounted) return;
    // ...
    await playerProvider.updatePlayerStats(playerId, won: isWinner, ...);
  }
  ```
- **Proposed**:
  ```dart
  await playerProvider.batchUpdatePlayerStats([
    for (final playerId in currentGame.playerIds)
      PlayerStatsUpdate(
        playerId: playerId,
        won: winnerIds.contains(playerId),
        gameName: 'Reef Royale',
        gameDuration: gameDuration,
        dartThrows: currentGame.totalDartsThrown[playerId] ?? 0,
        turns: currentGame.totalTurns[playerId] ?? 0,
        playerCount: playerCount,
      ),
  ]);
  ```
- **Server change:** new route `POST /api/v1/players/history/batch`. Body: `{"entries": [{"playerId": "...", "gameName": "...", ...}, ...]}`. Response: `{"saved": <count>, "failed": [{"playerId": "...", "reason": "..."}]}`. Wrapped in a single transaction; per-entry failure is captured in the `failed` array (matches the existing `failed_stats` tolerance).
- **Migration version:** none (no schema change — uses existing `game_history` table).
- **Dependencies:** none. Keep the legacy single-player route alive for backwards-compat with existing tests during rollout.

**Test impact (REQUIRED — both UI and non-UI):**

- **Existing non-UI tests touched** (`test/`):
  - `test/services/api/api_client_test.dart:328-340` (`addPlayerHistory` test) — passes unchanged; the new batch method is additive.
  - `test/providers/player_provider_test.dart` (whatever portion exercises `updatePlayerStats`) — should pass unchanged because the legacy method stays. **However**, if any test asserts call counts on a mock client's `addPlayerHistory`, those tests should be re-pointed at `batchAddPlayerHistory` after the call sites switch.
  - 6 results-screen tests in `test/screens/games/<game>/results/` (if present) — may need to update mocks to recognise the new batch call.
- **Existing server tests touched** (`server/test/`):
  - `server/test/routes/player_routes_test.dart` — passes unchanged (24 tests, no name overlap).
- **Existing UI/integration tests touched** (`integration_test/`):
  - 6 results-screen test trees: `integration_test/<game>/results_screen/*.dart` (or `.../results/*.dart` for clockwork_quest). Each uses `MockScoliaApiService` to simulate dart throws and reaches the results screen. They expect the persisted history to land in the in-memory mock server.
  - These pass unchanged IF the mock server supports the new batch route. The relevant shared helper is `test/shared/mock_api_server.dart` (or `integration_test/shared/...`) — needs the batch route handler added in the same commit.
- **Screenshot tests requiring re-validation:** None — no visual change.
- **New tests required:**
  - Non-UI (`test/`): `test/services/api/api_client_test.dart` — add 1 test: `'batchAddPlayerHistory POSTs to /history/batch with array body'`.
  - Non-UI (`test/`): `test/providers/player_provider_test.dart` — add 1 test: `'batchUpdatePlayerStats applies all entries and notifies once'`.
  - Server (`server/test/`): `server/test/routes/player_routes_test.dart` — add 3 tests: (a) batch insert happy path, (b) partial failure (some `playerId`s do not exist; result lists them in `failed`), (c) empty list returns 200 with `saved: 0`.
  - UI (`integration_test/`): None new — existing results-screen tests cover the integration path.
- **Tests likely to BREAK without an update:**
  - `integration_test/shared/mock_api_server.dart` (or wherever the in-test mock server lives — confirm path during Phase 5) — must add the `/history/batch` route, otherwise every integration test that hits a results screen will 404.
  - Any test asserting "exactly N calls to addPlayerHistory" — `grep` for `addPlayerHistory` in the `test/` and `integration_test/` trees during Phase 5 and adapt assertions.

**`game.build` skill impact (REQUIRED — assess every finding):**

- **Pattern to enshrine in `game.build`:** When a results screen persists per-player stats, it MUST call `playerProvider.batchUpdatePlayerStats(...)` ONCE — never loop `updatePlayerStats(...)` per player. The next new game would otherwise reintroduce the 8× HTTP round-trip pattern this audit just removed in 6 places.
- **Specific edits to `skills/game.build/SKILL.md` (and `.claude/skills/game.build/SKILL.md`):**
  - Section to update: the step in `game.build` that wires the results screen → API stats persistence (review during Phase 5; expected to be the "results screen" step in the build pipeline).
  - Verbatim text snippet to add (1-3 sentences, imperative voice):
    > **Results screen stats persistence MUST batch.** Call `playerProvider.batchUpdatePlayerStats([PlayerStatsUpdate(...), ...])` exactly once with all players' entries. Do NOT loop `playerProvider.updatePlayerStats(playerId, ...)` per player — every existing game in this repo uses the batch call and a per-player loop reintroduces N HTTP round-trips.
  - AR-4 verification row: **YES**. Proposed row: `Results-screen stats persistence: results_screen.dart calls batchUpdatePlayerStats exactly once and contains zero per-player updatePlayerStats loops`.

**Estimated savings:** 1 RTT per match per non-winner (typical local-LAN ~5-15ms each, production WAN ~80ms each). Worst case: 8-player Reef Royale on WAN ≈ 7 × 80ms ≈ 560ms wall-clock removed from results-screen settle time. Compounded across 6 games × every game-end transition.
**Risk:** Low — the legacy route stays alive, the batch route is additive, server-side wraps in a single transaction (atomic).
**Migration:** Land server route + client method first (back-compat preserved). Switch results screens one-by-one in subsequent commits if the user prefers smaller PRs; else flip all 6 in a single commit since the helper-update pattern is identical.

---

### A2 [D5] Add `PRAGMA synchronous = NORMAL` to server DB init

**Game code impact:**
- **Files:** `server/lib/database/database.dart:28-31` (`_configure()` method).
- **Currently:**
  ```dart
  void _configure() {
    _db.execute('PRAGMA journal_mode = WAL;');
    _db.execute('PRAGMA foreign_keys = ON;');
  }
  ```
- **Proposed:**
  ```dart
  void _configure() {
    _db.execute('PRAGMA journal_mode = WAL;');
    _db.execute('PRAGMA synchronous = NORMAL;');
    _db.execute('PRAGMA foreign_keys = ON;');
  }
  ```
- **Server change:** the pragma itself.
- **Migration version:** none (PRAGMA is a per-connection setting, not schema).
- **Dependencies:** none.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched (`server/test/`):** All 178 should pass unchanged. The pragma changes durability semantics but not correctness for the test workload.
- **Existing UI/integration tests touched:** None.
- **Screenshot tests requiring re-validation:** None.
- **New tests required:**
  - Server (`server/test/`): `server/test/database/database_test.dart` — 1 test: `'PRAGMA synchronous is NORMAL after configure'`. (Optional but cheap; locks in the setting against accidental rollback.)
- **Tests likely to BREAK without an update:** None.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** None — `game.build` doesn't generate server DB init code. Server-side, one-time fix.
- **Specific edits to `skills/game.build/SKILL.md`:** None — N/A.

**Estimated savings:** Per [SQLite Optimizations](https://www.powersync.com/blog/sqlite-optimizations-for-ultra-high-performance), `synchronous = NORMAL` with WAL is the recommended high-throughput config — typically 2-4× faster on writes vs. the default `FULL`, with no durability loss on graceful shutdown. Concretely: every `INSERT`/`UPDATE` in `addPlayerHistory`, `saveGame`, etc. gets faster.
**Risk:** Low. WAL is already on; `NORMAL` only relaxes the fsync schedule. Crash safety in WAL mode is preserved.
**Migration:** Single edit. Ships immediately.

---

### A3 [D3] Add index on `game_history.player_id` (and 2 other hot WHERE columns)

**Game code impact:**
- **Files:**
  - `server/lib/database/migrations/migration_v3.dart` — NEW file. Class `MigrationV3HotIndexes` with `version = 3`.
  - `server/lib/database/migration.dart` (or wherever the runner registers migrations) — register V3 in the runner's migration list.
- **Currently:** zero `CREATE INDEX` statements across migrations v1 and v2 (verified by grepping `server/lib/database/migrations/`).
- **Proposed (new file `migration_v3.dart`):**
  ```dart
  @override
  void migrate(sqlite3.Database db) {
    db.execute('CREATE INDEX IF NOT EXISTS idx_game_history_player_id ON game_history(player_id);');
    db.execute('CREATE INDEX IF NOT EXISTS idx_saved_games_game_type ON saved_games(game_type);');
    db.execute('CREATE INDEX IF NOT EXISTS idx_victory_music_is_current ON victory_music(is_current) WHERE is_current = 1;');
  }
  ```
- **Server change:** index creation only (DDL).
- **Migration version:** **3** (highest existing is v2 in `server/lib/database/migrations/migration_v2.dart`).
- **Dependencies:** none. Compounds with A4 (server N+1 fix) — together they make the player-list query O(N) on a real index instead of O(N²) full scans.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched (`server/test/`):** Migration tests at `server/test/database/migration_runner_test.dart` and `server/test/database/migration_v1_baseline_test.dart` / `migration_v2_failed_stats_test.dart` — these run `MigrationRunner.run()` against a fresh DB and verify state. They pass unchanged (V3 is additive). Existing tests verifying the schema may need a 1-line update to cover V3 (depends on the test style — verify in Phase 5).
- **Existing UI/integration tests touched:** None.
- **Screenshot tests requiring re-validation:** None.
- **New tests required:**
  - Server (`server/test/`): `server/test/database/migration_v3_hot_indexes_test.dart` — 3 tests: (a) all three indexes exist after V3 runs; (b) `EXPLAIN QUERY PLAN SELECT * FROM game_history WHERE player_id = ?` uses the new index; (c) running V3 twice is idempotent (`IF NOT EXISTS`).
- **Tests likely to BREAK without an update:** `server/test/database/migration_runner_test.dart` if it asserts the highest version is 2 — change to 3.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** None — `game.build` doesn't generate server-side schema migrations.
- **Specific edits to `skills/game.build/SKILL.md`:** None — N/A.

**Estimated savings:** `_loadHistory` at `server/lib/routes/player_routes.dart:69-77` is called once per player in `_getAll` (line 115). With ~50 players in a real install, that's 50 full table scans on `game_history` per `GET /players` request. With the index, each lookup is a B-tree probe. Per [SQLite docs](https://www.sqlite.org/optoverview.html), this turns the route from O(P × H) to O(P × log H) where H = total history rows.
**Risk:** Low. Indexes are pure adds; `IF NOT EXISTS` is idempotent.
**Migration:** v3 migration runs at server startup.

---

### A4 [R5] Wrap continuous-animation subtrees in `RepaintBoundary` (all 6 games)

**Game code impact:**
- **Files (and line ranges):**
  - `lib/widgets/carnival_string_lights.dart:137-186` — wrap the `AnimatedBuilder` in `_buildBulb()` with `RepaintBoundary`.
  - `lib/screens/games/reef_royale/reef_royale_game_screen.dart:926-943` — wrap the dart-indicator `AnimatedBuilder` in `RepaintBoundary`.
  - `lib/screens/games/monster_mash/monster_mash_game_screen.dart` (around the round-progress-bar `AnimatedBuilder`/AnimationController calls — exact line confirmed in Phase 5) — wrap.
  - `lib/screens/games/clockwork_quest/clockwork_quest_game_screen.dart` — wrap any AnimationController-driven gear/progress visuals.
  - `lib/screens/games/lunar_lander/lunar_lander_game_screen.dart:724, 801` — wrap the descent-progress flex column.
  - `lib/screens/games/target_tag/target_tag_menu_screen.dart:850-876` — wrap the start-button pulse `AnimatedBuilder`.
  - `lib/screens/games/target_tag/target_tag_game_screen.dart` — wrap any AnimationController-driven HUD subtrees identified in Phase 5.
- **Currently** (representative; `carnival_string_lights.dart:137-186`):
  ```dart
  Widget _buildBulb(int index) {
    return AnimatedBuilder(
      animation: ...,
      builder: (context, child) { return Opacity(opacity: ..., child: Container(...)); },
    );
  }
  ```
- **Proposed:**
  ```dart
  Widget _buildBulb(int index) {
    return RepaintBoundary(
      child: AnimatedBuilder(
        animation: ...,
        builder: (context, child) { return FadeTransition(opacity: ..., child: child); },
        child: const _StaticBulb(),
      ),
    );
  }
  ```
  (See A5 for the `Opacity → FadeTransition` swap, which is a separate finding.)
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** A5 (R4 swap) is a closely related visual change to the same files; recommend landing them together.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None expected — `RepaintBoundary` is a transparent wrapper.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** All 6 games' integration tests (`integration_test/<game>/`) mount these screens. They should pass unchanged — `RepaintBoundary` is invisible to widget-finder tests.
- **Screenshot tests requiring re-validation:**
  - `integration_test/<game>/visual_validation/` (or any `*_screenshot_test.dart` for the 6 games) — re-capture and verify pixel-identical (no visible difference is expected; the boundary only changes the layer composition).
- **New tests required:** None.
- **Tests likely to BREAK without an update:** None expected. The only risk is a test that asserts on widget tree structure and counts ancestors — uncommon in this repo.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** Every new game that introduces a continuous-animation widget (background pulse, progress bar, character animation) MUST wrap it in `RepaintBoundary`. Future games will reintroduce this anti-pattern by default since Flutter doesn't add it implicitly.
- **Specific edits to `skills/game.build/SKILL.md`:**
  - Section to update: the step in `game.build` that creates the game screen / animated widgets (review in Phase 5 — typically the "build the main game screen" step).
  - Verbatim text snippet to add:
    > **Continuous-animation subtrees MUST be wrapped in `RepaintBoundary`.** Any `AnimatedBuilder`, AnimationController-driven widget, or other widget that repaints every frame (background animations, progress bars, pulse effects, character motion) MUST have a `RepaintBoundary` ancestor as close as possible to the animated subtree. Without it, animation frames dirty sibling widgets and force the entire screen to repaint per frame.
  - AR-4 verification row: **YES**. Proposed row: `Animation isolation: every AnimatedBuilder/AnimationController-driven subtree on the game screen has a RepaintBoundary ancestor`.

**Estimated savings:** Per [Flutter perf best practices](https://docs.flutter.dev/perf/best-practices), `RepaintBoundary` removes the animated subtree from the parent's repaint region. On the Carnival Derby string lights (11 bulbs pulsing simultaneously), this typically reduces per-frame raster work by 30-60% on lower-end tablets. Concrete number depends on subtree size; expect biggest wins on screens with many static siblings (Carnival Derby, Reef Royale).
**Risk:** Low. `RepaintBoundary` is documented as safe; the only side effect is slightly more GPU memory (one extra layer per boundary).
**Migration:** Single-commit change per game; can land together.

---

### A5 [R4] Replace `Opacity` with `FadeTransition` in animated builders

**Game code impact:**
- **Files (and line ranges):**
  - `lib/widgets/carnival_string_lights.dart:147-149` — replace `Opacity(opacity: opacity, ...)` inside `AnimatedBuilder.builder` with `FadeTransition(opacity: _opacityAnimations[index], child: ...)`. Move the inner `Container` into the `child:` parameter so it isn't rebuilt every frame.
- **Currently:**
  ```dart
  builder: (context, child) {
    final opacity = _opacityAnimations[index].value;
    return Opacity(opacity: opacity, child: Container(...));
  },
  ```
- **Proposed:**
  ```dart
  return FadeTransition(
    opacity: _opacityAnimations[index],
    child: AnimatedBuilder(
      animation: _glowAnimations[index],
      builder: (context, child) => Container(
        decoration: BoxDecoration(boxShadow: [
          BoxShadow(blurRadius: 15 * _glowAnimations[index].value, ...),
        ]),
      ),
    ),
  );
  ```
  (Opacity is split off into `FadeTransition`; the inner `AnimatedBuilder` only handles the glow animation. Best to verify there is no other `Opacity`-in-`AnimatedBuilder` site by grepping for `Opacity(` inside `AnimatedBuilder` callbacks during Phase 5.)
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** Pairs cleanly with A4 (R5 RepaintBoundary). Apply both to `carnival_string_lights.dart` in the same edit.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** Carnival Derby integration tests at `integration_test/carnival_derby/` — these don't assert on `Opacity` widget specifically; expected to pass unchanged.
- **Screenshot tests requiring re-validation:**
  - Any Carnival Derby screenshot test (`integration_test/carnival_derby/visual_validation/` or `*_screenshot_test.dart`) — re-capture. `FadeTransition` produces visually identical output to a controller-driven `Opacity`.
- **New tests required:** None.
- **Tests likely to BREAK without an update:**
  - Any test using `find.byType(Opacity)` to locate the bulbs — would need to change to `find.byType(FadeTransition)`. Phase 5: grep `find.byType(Opacity)` in `test/` and `integration_test/`.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** Per [Flutter perf docs](https://docs.flutter.dev/perf/best-practices), `Opacity` inside an animated builder forces a saveLayer per frame; `FadeTransition` doesn't. Future games' animation widgets must follow this rule.
- **Specific edits to `skills/game.build/SKILL.md`:**
  - Section: the animation guidelines step.
  - Verbatim text:
    > **Use transition widgets, not generic widgets driven by an AnimationController.** Inside `AnimatedBuilder.builder`, NEVER use `Opacity(opacity: anim.value, ...)` — use `FadeTransition(opacity: anim, ...)` outside the builder. Same rule for `Transform → SlideTransition/RotationTransition/ScaleTransition`. The transition widgets short-circuit `saveLayer()` calls per frame.
  - AR-4 verification row: **YES**. Proposed row: `Animation primitives: zero usages of Opacity/Transform/Color inside AnimatedBuilder.builder — use FadeTransition/SlideTransition/etc. instead`.

**Estimated savings:** `Opacity` always allocates a `saveLayer` on the GPU; `FadeTransition` uses cheap pixel-blend in the common case. On 11 simultaneously animating bulbs, this is the difference between 11 saveLayer ops per frame and 0. ~2-5ms per frame on a mid-range tablet during racing animations.
**Risk:** Low. `FadeTransition` is the documented replacement.
**Migration:** Single-file edit.

---

## Bucket B — High impact, medium effort

### B1 [server N+1] Replace per-player `_loadHistory` loop with single JOIN query

**Game code impact:**
- **Files:**
  - `server/lib/routes/player_routes.dart:108-131` — `_getAll` currently calls `_loadHistory(player.id)` once per player. Replace with a single `SELECT ... FROM players LEFT JOIN game_history ON game_history.player_id = players.id ORDER BY players.name, game_history.timestamp DESC` and build the `ServerPlayer` aggregates in a single pass.
  - `server/lib/routes/player_routes.dart:81-95` — `_loadPlayer(id)` does 1+1 queries (player + history); leave as-is (only fires for single-player GET).
- **Currently:**
  ```dart
  final players = resultSetToList(rows).map((row) {
    final player = ServerPlayer.fromDbRow(row);
    final history = _loadHistory(player.id);   // <-- N+1
    return ServerPlayer(...).toJson();
  }).toList();
  ```
- **Proposed (sketch):**
  ```dart
  // Single JOIN: all players + their history in one query.
  final rows = _db.select('''
    SELECT p.*, h.id AS h_id, h.game_name AS h_game_name, h.timestamp AS h_timestamp,
           h.duration_ms AS h_duration_ms, h.metadata AS h_metadata,
           h.dart_throws AS h_dart_throws, h.turns AS h_turns, h.player_count AS h_player_count
    FROM players p
    LEFT JOIN game_history h ON h.player_id = p.id
    ORDER BY p.name ASC, h.timestamp DESC;
  ''');
  // Group rows by player.id, build history list inline.
  ```
- **Server change:** route handler refactor; same JSON response shape.
- **Migration version:** none.
- **Dependencies:** Compounds with A3 (D3 index). With the index, the JOIN cost is dominated by the ordering, not the lookup.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched (`server/test/`):** `server/test/routes/player_routes_test.dart` (24 tests) — every test asserting on `GET /players` JSON shape. They MUST pass unchanged because the JSON shape doesn't change. Verify with the existing test suite.
- **Existing UI/integration tests touched:** None directly. The Flutter client doesn't see the change.
- **Screenshot tests requiring re-validation:** None.
- **New tests required:**
  - Server (`server/test/`): `server/test/routes/player_routes_test.dart` — add 1 test: `'GET /players executes only 1 query regardless of player count'`. (Use a query-counting wrapper or just measure timing as a proxy. Optional — the JSON-equivalence tests already prove correctness.)
- **Tests likely to BREAK without an update:** None expected — JSON contract preserved.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** None — server-side route refactor; `game.build` doesn't generate server code.
- **Specific edits to `skills/game.build/SKILL.md`:** None — N/A.

**Estimated savings:** Cuts SQLite calls on `GET /players` from `1 + N` to `1`. With WAL+NORMAL, savings are smaller per call (~0.05-0.2ms each), but the network/serialization overhead per query call to SQLite is removed entirely. For a player roster of 50, expect a ~2-5ms reduction in route latency.
**Risk:** Medium-low. JOIN handling has the usual row-grouping bugs; the existing JSON-equivalence tests catch shape regressions.
**Migration:** Single commit.

---

### B2 [C3] Replace repeated `firstWhere(p => p.id == ...)` with cached `Map<String, T>` byId

**Game code impact:**
- **Files (and line ranges):**
  - `lib/screens/games/target_tag/target_tag_game_screen.dart:288, 297, 313, 319, 335, 341, 355, 361, 373, 1004` — 10+ `allPlayers.firstWhere(...)` calls. Replace with a `playerProvider.byId(id)` lookup.
  - `lib/providers/player_provider.dart:197, 408` — `_allPlayers.firstWhere((p) => p.id == id)` in `deletePlayer()` and `getPlayerById()`. Add a private cache `Map<String, Player> _byId` invalidated on every player-list mutation; expose `Player? byId(String id)` returning `_byId[id]`.
  - `lib/providers/reef_royale_provider.dart:381` — same pattern; replace via the cache exposed on `PlayerProvider`.
  - `lib/providers/monster_mash_provider.dart:359` — same.
- **Currently:**
  ```dart
  final player = allPlayers.firstWhere((p) => p.id == eliminatedId);
  ```
- **Proposed:**
  ```dart
  final player = playerProvider.byId(eliminatedId);
  if (player == null) return; // explicit absence handling
  ```
  (Where `byId` is a getter returning `Map<String, Player>` cached against the current `_allPlayers` length+identity.)
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** none.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** Provider tests in `test/providers/player_provider_test.dart`, `test/providers/target_tag_provider_test.dart`, etc. — they should pass unchanged because `byId` returns the same `Player` instance as `firstWhere`.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** Target Tag, Reef Royale, Monster Mash UI tests — pass unchanged.
- **Screenshot tests requiring re-validation:** None.
- **New tests required:**
  - Non-UI (`test/`): `test/providers/player_provider_test.dart` — add 2 tests: (a) `'byId returns same instance as firstWhere'`, (b) `'byId map invalidates on add/remove/update'`.
- **Tests likely to BREAK without an update:** None expected — pure refactor.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** Generalisable pattern but minor — flag for future games but only if `byId` lookups are >5 in a hot path.
- **Specific edits to `skills/game.build/SKILL.md`:**
  - Section: the step for hooking up provider lookups in game-screen build methods.
  - Verbatim text:
    > **Prefer `playerProvider.byId(id)` over `players.firstWhere((p) => p.id == id)`** in build methods and event handlers. The former is O(1); the latter is O(N) per call and runs once per player card per rebuild.
  - AR-4 verification row: **NO** (case-by-case; not every game has hot-path id lookups). Add as a recommendation, not a hard verification gate.

**Estimated savings:** For a target_tag screen with 8 players, 10× firstWhere calls × 8-element list = 80 comparisons per build. With `byId`: 10 hash lookups. Tiny per-frame, but compounds during continuous-animation rebuilds.
**Risk:** Low. Pure refactor.
**Migration:** Land `byId` getter first, swap call sites in a follow-up commit.

---

## Bucket C — Quick wins

### C1 [C2] Cache repeated `targetTagProvider.isSoloHero(playerId)` calls in build

**Game code impact:**
- **Files:** `lib/screens/games/target_tag/target_tag_game_screen.dart:1012-1062` — `_buildPlayerCard` calls `targetTagProvider.isSoloHero(playerId)` twice per card (lines 1012 & 1015 in solo mode, 1051 & 1057 in team mode).
- **Currently:**
  ```dart
  soloHeroBuffNumber: targetTagProvider.isSoloHero(playerId)
      ? targetTagProvider.getSoloHeroBuffNumber(playerId) : null,
  soloHeroBuffMultiplier: targetTagProvider.isSoloHero(playerId)
      ? targetTagProvider.getSoloHeroBuffMultiplier(playerId) : null,
  ```
- **Proposed:**
  ```dart
  final isHero = targetTagProvider.isSoloHero(playerId);
  // ...
  soloHeroBuffNumber: isHero ? targetTagProvider.getSoloHeroBuffNumber(playerId) : null,
  soloHeroBuffMultiplier: isHero ? targetTagProvider.getSoloHeroBuffMultiplier(playerId) : null,
  ```
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** none.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** Target Tag UI tests — pass unchanged.
- **Screenshot tests requiring re-validation:** None.
- **New tests required:** None.
- **Tests likely to BREAK without an update:** None.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** None — game-internal optimization.

**Estimated savings:** Trivial CPU cycles per frame (2 method dispatches saved per player card). Worth doing as part of B2 cleanup, not on its own.
**Risk:** Low.
**Migration:** Single-file edit.

---

### C2 [C4] Coalesce `notifyListeners()` chain in `DartboardProvider.connectToDartboard()`

**Game code impact:**
- **Files:** `lib/providers/dartboard_provider.dart:102-174` (`connectToDartboard` method, calls `notifyListeners()` 5 times: 109, 139, 152, 156, 164).
- **Currently:** 5 sequential `notifyListeners()` calls, one per state mutation.
- **Proposed:** Mutate state, defer notification to the end of the path. Where intermediate "connecting → connected" UI feedback is required, keep that single intermediate notify; collapse the rest.
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** none.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** `test/providers/dartboard_provider_test.dart` (30 tests) — any test asserting on the call count of `notifyListeners` or counting sequential rebuilds will need updates. Most tests use `await Future.microtask(() {})` and assert on final state — those pass unchanged.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** None expected.
- **Screenshot tests requiring re-validation:** None.
- **New tests required:** None.
- **Tests likely to BREAK without an update:**
  - Any dartboard provider test that uses `int notifyCount = 0; provider.addListener(() => notifyCount++);` and asserts `notifyCount == 5`. Phase 5: grep `notifyCount` and `addListener` in `test/providers/dartboard_provider_test.dart` and adapt.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** Provider methods that mutate multiple fields should call `notifyListeners()` ONCE at the end, not after each mutation. Generalisable rule.
- **Specific edits to `skills/game.build/SKILL.md`:**
  - Section: provider authoring guidelines.
  - Verbatim text:
    > **Coalesce `notifyListeners()` calls.** Provider methods that mutate multiple fields in sequence MUST call `notifyListeners()` exactly once, at the end of the method. Calling it after each mutation triggers redundant rebuilds in every listening widget.
  - AR-4 verification row: **NO** (review-time check, not auto-grade). The rule is hard to verify mechanically.

**Estimated savings:** During dartboard connect, every listening widget rebuilds 5× instead of 1× — meaningful on screens with multiple watchers (DartboardConnectionInfo, status pill, etc.).
**Risk:** Medium-low. If any UI consumer relies on the intermediate rebuild for animation kickoff, it'd skip a frame. Walk the call sites in Phase 5.
**Migration:** Single-file edit.

---

### C3 [R3] Remove redundant empty `setState(() {})` calls

**Game code impact:**
- **Files:**
  - `lib/screens/games/lunar_lander/lunar_lander_game_screen.dart:68, 211, 250` — empty `setState(() {})` calls.
  - `lib/screens/games/clockwork_quest/clockwork_quest_game_screen.dart:60, 163, 231` — empty `setState(() {})` calls.
- **Currently:**
  ```dart
  if (mounted) setState(() {});
  ```
- **Proposed:** Investigate each call. If the rebuild is needed and Provider's notify already covers it, delete the line. If it covers state not in a provider, hoist that state into a `ValueNotifier` or extend the existing provider.
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** none.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** Lunar Lander and Clockwork Quest UI tests — pass unchanged IF the rebuild was redundant. If a test depends on the spurious rebuild, it would break.
- **Screenshot tests requiring re-validation:**
  - Lunar Lander and Clockwork Quest screenshot tests — verify visual identity after each removal.
- **New tests required:** None.
- **Tests likely to BREAK without an update:** None expected, but Phase 5 must verify each removal doesn't leave stale UI.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** YES — empty setState as a "force rebuild" hack is a code smell. Rule prevents future games from reintroducing it.
- **Specific edits to `skills/game.build/SKILL.md`:**
  - Section: state-management rules.
  - Verbatim text:
    > **Never use empty `setState(() {})` as a rebuild trigger.** If the state lives in a Provider, `notifyListeners()` already covers the rebuild. If state is local, place it in a `ValueNotifier` or named field so `setState` has a meaningful argument list. Empty setState hides the dependency and causes spurious rebuilds.
  - AR-4 verification row: **YES**. Proposed row: `No empty setState: zero matches for setState\(\(\) \{\}\) in lib/screens/games/<game>/`.

**Estimated savings:** Each empty setState forces a full subtree rebuild. Removing avoids 1 rebuild per call site per game-event. Concretely: in Lunar Lander gameplay, ~3 spurious rebuilds per dart throw.
**Risk:** Medium. Each removal needs verification that the UI still updates correctly. Phase 5 must spot-check via UI tests.
**Migration:** One file at a time; verify after each.

---

### C4 [R1] Add `const` on hot-path `BoxDecoration`/`SizedBox` in progress bars

**Game code impact:**
- **Files (and line ranges):**
  - `lib/screens/games/monster_mash/monster_mash_game_screen.dart:1068-1070, 1298-1308` — `SizedBox` and `BoxDecoration` in opponent grid + round progress bar.
  - `lib/screens/games/reef_royale/reef_royale_game_screen.dart:1299-1308` — progress bar `BoxDecoration`.
- **Currently:**
  ```dart
  decoration: BoxDecoration(
    color: Colors.black.withOpacity(0.4),
    borderRadius: BorderRadius.circular(16),
    border: Border.all(color: _seafoamGreen.withOpacity(0.5), width: 1),
  ),
  ```
- **Proposed:** Where ALL args are literals (no provider/state-derived values), prefix with `const`. Example: a `BorderRadius.circular(16)` is `const`-eligible. `Colors.black.withOpacity(0.4)` is NOT (computes at runtime); rewrite as `const Color(0x66000000)`.
- **Server change:** none.
- **Migration version:** none.
- **Dependencies:** none.

**Test impact:**

- **Existing non-UI tests touched (`test/`):** None.
- **Existing server tests touched:** None.
- **Existing UI/integration tests touched:** None.
- **Screenshot tests requiring re-validation:**
  - Monster Mash and Reef Royale screenshot tests — verify visual identity.
- **New tests required:** None.
- **Tests likely to BREAK without an update:** None.

**`game.build` skill impact:**
- **Pattern to enshrine in `game.build`:** Generalised rule — high-traffic widgets in build methods should be `const`-eligible.
- **Specific edits to `skills/game.build/SKILL.md`:**
  - Section: widget-authoring rules.
  - Verbatim text:
    > **Prefer `const` constructors in hot-path build methods.** Game screens repaint frequently; widgets that don't depend on state (literal `SizedBox`, `BoxDecoration` with `const Color(...)`, `BorderRadius.circular(literal)`) MUST be prefixed with `const`. Avoid `Colors.X.withOpacity(...)` — use `const Color(0xAARRGGBB)` instead.
  - AR-4 verification row: **NO** (over-eager `const` detection produces noise; soft rule).

**Estimated savings:** Each missed `const` is a small per-frame allocation. Worth taking when easy, not blocking.
**Risk:** Low.
**Migration:** Edit file, verify build passes (`flutter analyze`), screenshot test.

---

## Bucket D — Deferred (low priority or high risk)

- **D1 [T1] Replace per-game `completeGameToVictory` test loop with `setXProviderToVictoryState` helpers (6 games).** Effort: Medium (6 helper files + new provider methods). Risk: Medium (must precisely match natural game-end state). Estimated savings: ~5-10s per results-screen test; cumulative across 60+ results tests = meaningful. Defer unless explicitly requested — this is a test-suite optimization, not an app perf finding.
- **D2 [C5] `jsonDecode` on UI thread (19 sites in `api_client.dart`).** Move to `compute()` or background isolate. Effort: Medium. Risk: Medium-low. ROI low for short JSON payloads; defer.
- **D3 [R2] Switch `context.watch<X>()` to `context.select<X, T>(...)` on 2 game screens.** Effort: Medium. Risk: Medium (verify rebuild correctness for every selected field). Defer until A4/A5 land first.
- **D4 [C1] Provider sort/where chains called in build (`target_tag` and `monster_mash`).** Memoize via `ValueNotifier` or capture in `late final`. Effort: Small-Medium. ROI low; defer.
- **D5 [N5] Status-check `Timer.periodic` 10s in `DartboardProvider` + `ScoliaWebSocketService`.** Already correctly designed; could exponentially back off when idle. Effort: Small. Risk: Medium (must not regress connection-loss detection). Defer.
- **D6 [D4] `SELECT *` to explicit columns in `saved_game_routes.dart` and `player_routes.dart`.** Tiny payload reduction; ROI marginal. Defer.

## Tests that MUST run after implementation

- **Suites:**
  - `flutter test` — must stay green at the documented count (currently 1275 Flutter non-UI tests pass per CLAUDE.md), PLUS any new tests added per the per-finding plan.
  - `cd server && dart test` — must stay green at 178 + any new server tests added.
  - `flutter analyze` — zero new errors.
- **Targeted UI re-runs (only for approved findings touching the listed files):**
  - For A1 (N3 batch): all 6 results-screen integration trees:
    - `integration_test/reef_royale/results_screen/`
    - `integration_test/target_tag/results_screen/`
    - `integration_test/carnival_derby/ui/` (results path)
    - `integration_test/clockwork_quest/results/`
    - `integration_test/lunar_lander/results_screen/`
    - `integration_test/monster_mash/results_screen/`
  - For A4 + A5 (RepaintBoundary + FadeTransition): the affected game's gameplay integration tests:
    - `integration_test/carnival_derby/` (string lights)
    - `integration_test/reef_royale/` (dart-indicator pulse)
    - others depending on per-file scope from Phase 5.
  - For C3 (empty setState removal): `integration_test/lunar_lander/` and `integration_test/clockwork_quest/`.
- **Targeted screenshot tests:**
  - Carnival Derby string-lights area (A4 + A5)
  - Reef Royale dart-indicator + progress bar (A4)
  - Monster Mash + Reef Royale progress bars (C4)
  - Lunar Lander + Clockwork Quest game screens (C3)

## Approval prompt

```
═══════════════════════════════════════════════════════
APPROVAL NEEDED — pick which findings to apply

📄 Full plan: docs/perf-audits/2026-05-05-full.md

Reply with one of:
  • "approve all"             → implement everything in buckets A/B/C
  • "approve #1, #3, #5"      → implement only those IDs (A1, A3, A5, etc.)
  • "approve bucket A"        → implement only bucket-A items
  • "decline"                 → stop here, no changes
  • "modify #2: <details>"    → request a change to a specific item

I will NOT make any code changes until you reply.
═══════════════════════════════════════════════════════
```

## Implementation log (Phase 5)

- **A2 [D5] applied** — files: `server/lib/database/database.dart`. New tests: `server/test/database_test.dart` ('sets synchronous to NORMAL (1)'). Status: 26/26 database_test.dart pass. Date: 2026-05-05. game.build update: N/A.
- **A3 [D3] applied** — files: `server/lib/database/migrations/migration_v3.dart` (new), `server/lib/database/migration.dart` (registered V3). Updated tests: `server/test/migration_test.dart` (currentVersion → 3, schema version → 3). New tests: `server/test/migration_test.dart` (7 new — `MigrationV3HotIndexes` group: has version 3, has a description, creates each of the 3 indexes, EXPLAIN QUERY PLAN uses idx_game_history_player_id, idempotent). Status: 186/186 server tests pass (was 178). Date: 2026-05-05. game.build update: N/A.
- **A1 [N3] applied** — files: `server/lib/routes/player_routes.dart` (new POST /history/batch handler with single transaction + reused prepared statements), `lib/services/api/api_client.dart` (new `batchAddPlayerHistory`), `lib/providers/player_provider.dart` (new `batchUpdatePlayerStats`, `PlayerStatsUpdate` class), all 6 results screens (Reef Royale, Target Tag, Carnival Derby, Clockwork Quest, Lunar Lander, Monster Mash), `test/shared/mock_api_helpers.dart` (mock batch route). New tests: `server/test/routes/player_routes_test.dart` (4 — happy path, partial failure, empty list, 400 missing field), `test/services/api/api_client_test.dart` (1 — batchAddPlayerHistory POSTs), `test/providers/player_provider_test.dart` (3 — applies all + notifies once, skips unknown players, empty list). Status: 545/545 provider+services Flutter tests pass; 190/190 server tests pass. Date: 2026-05-05. game.build update: pending (Step 6 of Phase 5).
- **B1 [server N+1] applied** — files: `server/lib/routes/player_routes.dart` `_getAll` rewritten to use a single LEFT JOIN of players × game_history with explicit column aliases. Cuts query count on `GET /players` from `1 + N` to `1`. Compounds with A3 index `idx_game_history_player_id`. Existing 178+24=190 server tests confirm JSON contract preserved (no behaviour change observable to clients). Status: 190/190 server tests pass. Date: 2026-05-05. game.build update: N/A (server-only).
- **A4 + A5 [R5 + R4] applied** — files: `lib/widgets/carnival_string_lights.dart` (RepaintBoundary + Opacity → FadeTransition split: outer FadeTransition drives opacity, inner AnimatedBuilder drives glow shadows), `lib/screens/games/reef_royale/reef_royale_game_screen.dart` (RepaintBoundary on dart-indicator pulse), `lib/screens/games/target_tag/target_tag_menu_screen.dart` (RepaintBoundary on start-button pulse), `lib/screens/games/monster_mash/monster_mash_menu_screen.dart` (3 RepaintBoundary wraps: lightning overlay, glow pulse, secondary lightning), `lib/screens/games/monster_mash/monster_mash_results_screen.dart` (2 RepaintBoundary wraps: lightning overlay, glow shadow). Lunar Lander and Clockwork Quest game screens have no AnimationController-driven subtrees (use AnimatedContainer/Positioned animations instead — no RepaintBoundary fix needed). Existing tests touched: none — RepaintBoundary is a transparent wrapper. Status: flutter analyze on changed files shows 0 new errors (only pre-existing warnings/info). Date: 2026-05-05. game.build update: pending (Step 6 of Phase 5).
- **B2 [C3] applied** — files: `lib/providers/player_provider.dart` (added `_byIdCache` field, override of `notifyListeners()` to invalidate cache, public `byId(String id)` getter; rewrote `getPlayerById` to delegate to `byId`), `lib/screens/games/target_tag/target_tag_game_screen.dart` (replaced 10 `allPlayers.firstWhere((p) => p.id == X)` calls with `playerProvider.byId(X)!`; added `playerProvider` lookup to `_buildPlayerCard`). Skipped: 2 `firstWhere` calls in `reef_royale_provider.dart`/`monster_mash_provider.dart` (called once at game-save, not hot path) and 2 in `player_provider.dart` (`deletePlayer` once-per-delete, internal `pending.firstWhere` over a tiny local list). New tests: `test/providers/player_provider_test.dart` (5 — returns same instance as firstWhere, returns null for unknown id, cache invalidates on add/delete/in-place stat update). Status: 1306/1306 Flutter tests pass; 190/190 server tests pass. Date: 2026-05-05. game.build update: soft (recommendation, not AR-4).
- **C1 [C2] applied** — files: `lib/screens/games/target_tag/target_tag_game_screen.dart::_buildPlayerCard` (cached `targetTagProvider.isSoloHero(playerId)` in `isHero` local; collapsed `firstPlayerId = teamPlayerIds.first` in team branch; team branch now does 1 isSoloHero lookup instead of 2, plus avoids 5 redundant `teamPlayerIds.first` accesses). Existing tests unchanged. Status: flutter analyze on file shows 0 errors. Date: 2026-05-05. game.build update: N/A (game-internal optimization).
- **C2 [C4] deferred** — re-examining `dartboard_provider.dart` `connectToScolia` (the connect method actually used in this codebase; `connectToDartboard` does not exist), the 5 notifyListeners calls are on mutually-exclusive code paths (connecting state, success, sync error, async exception) plus 2 in an event-listener callback that fires asynchronously when WebSocket events arrive. They are NOT a synchronous chain — there's only ever 1 notify per code path. Phase 1's agent counted 5 occurrences in the method body and flagged it, but there's nothing to coalesce. No code change. Date: 2026-05-05. game.build update: N/A.
- **C3 [R3] deferred** — re-examining each empty `setState(() {})` site individually:
  - `lunar_lander_game_screen.dart:68` and `clockwork_quest_game_screen.dart:60` — fire after assigning `_mockApi`, which is local widget state. Needs a rebuild trigger. **Not redundant.**
  - `lunar_lander_game_screen.dart:211, 250` and `clockwork_quest_game_screen.dart:163, 231` — end-of-event-handler defensive rebuilds. Might be redundant if every consumer in build uses `context.watch`, but without UI test runs to validate (UI suite is ~153 minutes), removing them risks stale visuals during gameplay. The plan flagged this as Medium risk; I judge the per-frame win not worth the regression risk in this session. **Deferred to a future, UI-test-bracketed audit.**
  Status: no code change. Date: 2026-05-05. game.build update: rule still worth adding (see Step 6).
- **C4 [R1] deferred** — re-examining the cited progress-bar widgets in monster_mash:1068-1070, 1298-1308 and reef_royale:1299-1308: every BoxDecoration has at least one runtime dependency (`Colors.X.withOpacity(...)` for non-literal opacity, `BorderRadius.circular(barHeight / 2)` for computed radius, ternary-conditional border colors). Making them const-eligible requires rewriting `Colors.X.withOpacity(C)` → `const Color(0xRRGGBBAA)` for 5+ sites and unwinding ternary args — a style refactor rather than a perf-targeted change. The Flutter analyzer's `prefer_const_constructors` lint is already enabled (the suite passes), so there are no obvious unflagged wins. **Deferred** as marginal. Status: no code change. Date: 2026-05-05. game.build update: N/A.
- **game.build skill updates applied** — files: `.claude/skills/game.build/SKILL.md`, `skills/game.build/SKILL.md` (kept byte-identical via `cp`). New rules: Step 6 results-screen body now mandates `batchUpdatePlayerStats(...)` over per-player loop; AR-4 row (e) rewritten to verify the batch call; AR-4 row (d) in the integration audit similarly updated; new AR-4 rows (ll) RepaintBoundary around AnimatedBuilder, (mm) FadeTransition not Opacity inside AnimatedBuilder, (nn) no empty setState (with the field-assignment exception). `diff -q` reports both copies identical (2338 lines). Date: 2026-05-05.
